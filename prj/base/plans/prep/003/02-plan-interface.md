# Plan 2: Define the minimal plan interface

1. Add a failing test for the smallest stable plan result contract.
2. Implement a deterministic, typed result without execution policy.
3. Separate public types from implementation details, preserve the tested API, and commit.

Provide the step summary, test result, commit, and affected paths as inputs to the final Telegram completion summary.
