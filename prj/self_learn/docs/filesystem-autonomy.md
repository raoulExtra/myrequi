# Filesystem autonomy

The self_learn project uses the filesystem as its working surface.

## rule
- Keep the directory tree readable.
- Prefer small markdown files.
- Capture new learnings as docs, plans, or examples.
- Verify structure with `prj_tool.py`.

## loop
observe -> update -> verify -> reuse
