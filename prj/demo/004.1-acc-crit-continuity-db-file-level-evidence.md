'''yaml
title: 004.1-acc-crit-continuity-db-file-level-evidence
requirement_id: R4.1
source_requirement: 004.1-requi-continuity-db-file-level-evidence.md
'''

# Acceptance criteria for R4.1

## Requirement
- [R4.1 requirement](004.1-requi-continuity-db-file-level-evidence.md)

## Verification ideas
- The evidence file exists at `prj/base/docs/memory-and-filesystem-guidance.md`.
- The evidence file mentions `continuity.db`.
- The evidence file names filesystem-resident project material.
- The evidence file mentions `starter.md`.
- The R4 requirement file contains term[REF_EXISTS] references for the evidence values.
- The marker syntax places the marker directly after the term.
- The Python checker accepts `--root`.
- The Python checker accepts `--path`.
- The Python checker accepts `--requi`.
- The Python checker accepts `--evidence`.
- The Python checker accepts `--filesystem-cue`.
- The Python checker accepts `--marker`.
- The Python checker accepts `--link-target`.
- The Python checker accepts `--link-file`.
- The Python checker accepts `--json`.
- The Python checker accepts `--json-in`.
- The Python checker accepts `-v` / `--version`.
