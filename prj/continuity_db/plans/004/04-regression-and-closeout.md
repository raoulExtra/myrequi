# Part 04: Regression and closeout

1. Run focused semantic-model tests.
2. Run the relevant migration, view, helper, and route regressions.
3. Verify counts, foreign keys, views, triggers, and schema version.
4. Review unresolved overlap candidates and document them explicitly.
5. Commit each green logical step, then record `END_CONDITION_MET` only when
   every acceptance criterion is evidenced.
6. Apply the terminal logfile protocol as the final action.
